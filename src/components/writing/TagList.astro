---
import TechBadge from "@components/ui/TechBadge.astro";
interface Props {
  tags: Array<{ tag: string; count?: number }>;
  active?: string;
  class?: string;
}
const { tags, active, class: className } = Astro.props;
---

<nav aria-label="Tags" class:list={["flex flex-wrap gap-2", className]}>
  <TechBadge label="All" size="md" tone={active ? "neutral" : "accent"} href="/writing" />
  {tags.map(({ tag, count }) => (
    <TechBadge
      label={count ? `${tag} (${count})` : tag}
      size="md"
      tone={active === tag ? "accent" : "neutral"}
      href={`/writing/tags/${tag}`}
    />
  ))}
</nav>
